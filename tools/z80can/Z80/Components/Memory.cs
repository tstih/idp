using Z80.Interfaces;

namespace Z80.Components
{
    /*
    public class Memory : IMemory
    {
        protected const ushort MEM_SIZE = 0xffff;

        protected byte[] _memory;

        public Memory()
        { _memory = new byte[MEM_SIZE]; }

        public virtual byte Read(
            ushort in_address, 
            bool in_m1_state,
            bool in_opcode)
        { return _memory[in_address]; }

        public virtual void Write(ushort in_address, byte in_value)
        { _memory[in_address] = in_value; }
    }
    */
}