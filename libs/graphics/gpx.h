/*
 * gpx.h
 * drawing primitives, using absolute coordinates and clipping.
 * 
 * tomaz stih thu mar 25 2021
 */
#ifndef _GPX_H

/*
extern void gpx_draw_line(point_t start, int x, int y, int x2, int y2, rect_t *clip);
extern void gpx_fill_rectangle(rect_t rectangle, rect_t *clip);
extern void gpx_draw_rectangle(rect_t rectangle, rect_t *clip);
extern void gpx_draw_text(font_t *font, char* text, int x, int y, rect_t *clip);
extern void gpx_put_sprite(sprite_t *sprite, int x, int y, rect_t *clip);
extern void gpx_get_sprite(sprite_t *sprite, int x, int y, uint8_t *buffer, rect_t *clip);
*/

#define _GPX_H
#endif // _DRAW_H